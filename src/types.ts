export type ProductList = {
	productListId: string
	month: string
	created_at: Date
	user_Id: string
}

export type Item = {
	itemId: string
	created_at: Date
	name: string
	amount: string
	price: string
}
